services:
  discord-app:
    image: ${IMG_NAME}
    container_name: discord-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${STATUS_SERVER_PORT:-8443}:${STATUS_SERVER_PORT:-8443}"